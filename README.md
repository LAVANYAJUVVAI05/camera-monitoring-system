# camera-monitoring-system
Developed a FastAPI-based camera monitoring dashboard that manages surveillance cameras across multiple locations. The system uses SQLite for data storage and provides a web interface to view, update, and add camera details such as location, camera type (NVR, SIM, MATRIX), and operational status (online/offline).
